apiVersion: v1
kind: Service
metadata:
  name: demo-app
  labels:
    app: demo-app
spec:
  ports:
    - port: 80
      protocol: TCP
      name: app
      targetPort: 8080
#  sessionAffinity: ClientIP
  selector:
    app: demo-app